{
  "name": "spudguard",
  "version": "2.0.0",
  "private": true,
  "description": "Full-stack potato disease classification with Google auth and AI advice",
  "scripts": {
    "install:all": "cd frontend && npm install",
    "start:api": "cd api && python main.py",
    "start:frontend": "cd frontend && npm start",
    "start": "concurrently -n api,web -c green,blue \"npm run start:api\" \"npm run start:frontend\"",
    "stop": "powershell -ExecutionPolicy Bypass -File ./stop.ps1"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
